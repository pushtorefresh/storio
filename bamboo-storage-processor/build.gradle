apply plugin: 'java'

targetCompatibility = '1.6'
sourceCompatibility = '1.6'

dependencies {
    // Common code
	compile project(':bamboo-storage-common')

    // @AutoService annotation, creates META-INF bla bla
    compile 'com.google.auto.service:auto-service:1.0-rc2'
}